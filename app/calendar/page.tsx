import { requireUser } from '@/lib/auth';import { supabaseAdmin } from '@/lib/supabaseAdmin';import { Nav } from '@/components/Nav';
export default async function Calendar(){await requireUser();const {data:clips=[]}=await supabaseAdmin.from('clips').select('*').not('scheduled_at','is',null).order('scheduled_at');return <><Nav/><h1 className="text-4xl font-black mb-4">Calendar</h1><div className="card"><table className="w-full text-left"><tbody>{(clips as any[]).map(c=><tr key={c.id} className="border-b border-slate-700"><td className="py-3">{new Date(c.scheduled_at).toLocaleString()}</td><td>{c.youtube_title||c.filename}</td><td>{c.status}</td></tr>)}</tbody></table></div></>}
