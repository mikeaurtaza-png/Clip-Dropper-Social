import { requireUser } from "@/lib/auth";
import { listRows } from "@/lib/supabaseRest";
import type { Clip } from "@/lib/types";
export default async function Clips(){ await requireUser(); const clips=await listRows<Clip>("clips","order=created_at.desc&limit=100"); return <main><div className="card" style={{padding:22,marginBottom:18}}><h1>Clip Queue</h1><form action="/api/clips" method="post" className="grid2"><div><label>Filename</label><input className="input" name="filename" placeholder="kittle-update.mp4" required/></div><div><label>Dropbox path or video URL</label><input className="input" name="video_url" placeholder="https://... or /Shorts Ready/file.mp4"/></div><button className="btn">Add Clip</button></form></div><div className="card" style={{padding:22}}><h2>All Clips</h2>{clips.length===0?<p>No clips found.</p>:clips.map(c=><article key={c.id} style={{borderTop:'1px solid rgba(148,163,184,.18)',padding:'16px 0'}}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><div><h3 style={{margin:'0 0 4px'}}>{c.filename}</h3><p style={{margin:0,color:'#94a3b8'}}>{c.youtube_title || 'No title yet'}</p></div><b>{c.status}</b></div><div className="grid2" style={{marginTop:12}}><form action="/api/ai/generate" method="post"><input type="hidden" name="clip_id" value={c.id}/><button className="btn secondary">Generate AI Text</button></form><form action={`/api/clips/${c.id}`} method="post"><label>Schedule time</label><input className="input" type="datetime-local" name="scheduled_at"/><input type="hidden" name="status" value="scheduled"/><button className="btn secondary" style={{marginTop:8}}>Schedule</button></form></div><details style={{marginTop:12}}><summary>Edit metadata</summary><form action={`/api/clips/${c.id}`} method="post" style={{marginTop:12}}><label>YouTube Title</label><input className="input" name="youtube_title" defaultValue={c.youtube_title||''}/><label>YouTube Description</label><textarea name="youtube_description" rows={4} defaultValue={c.youtube_description||''}/><label>YouTube Tags</label><textarea name="youtube_tags" rows={3} defaultValue={c.youtube_tags||''}/><label>Facebook Caption</label><textarea name="facebook_caption" rows={4} defaultValue={c.facebook_caption||''}/><button className="btn" style={{marginTop:10}}>Save</button></form></details>{c.youtube_url&&<p><a className="btn secondary" href={c.youtube_url}>Open YouTube</a></p>}{c.facebook_url&&<p><a className="btn secondary" href={c.facebook_url}>Open Facebook</a></p>}{c.error&&<p style={{color:'#fca5a5'}}>Error: {c.error}</p>}</article>)}</div></main> }
