import { NextRequest,NextResponse } from 'next/server';import { env } from '@/lib/env';import { googleRedirect } from '@/lib/oauth';import { setUserCookie } from '@/lib/auth';
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get('code')!;const tr=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:env.googleClientId,client_secret:env.googleClientSecret,redirect_uri:googleRedirect,grant_type:'authorization_code'})});const tokens=await tr.json();const ur=await fetch('https://www.googleapis.com/oauth2/v2/userinfo',{headers:{Authorization:`Bearer ${tokens.access_token}`}});const user=await ur.json();if(user.email!==env.allowedEmail)return NextResponse.redirect(`${env.appUrl}/login?denied=1`);await setUserCookie({email:user.email,name:user.name});return NextResponse.redirect(`${env.appUrl}/dashboard`)}
