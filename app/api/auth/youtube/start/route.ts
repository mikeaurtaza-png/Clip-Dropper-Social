import { NextResponse } from "next/server"; import { appUrl, env } from "@/lib/env"; import { requireUser } from "@/lib/auth";
export async function GET(){ await requireUser(); const id=env('GOOGLE_CLIENT_ID'); if(!id) return NextResponse.redirect(`${appUrl()}/settings?google_missing=1`); const scope=encodeURIComponent('https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/youtube.readonly'); const red=encodeURIComponent(`${appUrl()}/api/auth/youtube/callback`); return NextResponse.redirect(`https://accounts.google.com/o/oauth2/v2/auth?client_id=${id}&redirect_uri=${red}&response_type=code&scope=${scope}&access_type=offline&prompt=consent`); }
