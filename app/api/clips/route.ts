import { NextResponse } from 'next/server';import { supabaseAdmin } from '@/lib/supabaseAdmin';
export async function POST(req:Request){const f=await req.formData();const filename=String(f.get('filename')||'');const dropbox_path=String(f.get('dropbox_path')||'');const {error}=await supabaseAdmin().from('clips').insert({filename,dropbox_path,status:'draft'});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.redirect(new URL('/clips',req.url));}
