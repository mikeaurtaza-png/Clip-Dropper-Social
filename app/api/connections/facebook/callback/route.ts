import { NextRequest,NextResponse } from 'next/server';import { env } from '@/lib/env';import { facebookRedirect } from '@/lib/oauth';import { supabaseAdmin } from '@/lib/supabaseAdmin';
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get('code')!;const u=new URL('https://graph.facebook.com/v20.0/oauth/access_token');u.searchParams.set('client_id',env.facebookClientId);u.searchParams.set('client_secret',env.facebookClientSecret);u.searchParams.set('redirect_uri',facebookRedirect);u.searchParams.set('code',code);const tokens=await (await fetch(u)).json();await supabaseAdmin.from('connections').upsert({provider:'facebook',tokens,status:'connected',updated_at:new Date().toISOString()},{onConflict:'provider'});return NextResponse.redirect(`${env.appUrl}/settings`)}
