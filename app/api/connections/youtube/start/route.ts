import { NextResponse } from 'next/server';import { env } from '@/lib/env';import { oauthUrl,youtubeRedirect } from '@/lib/oauth';
export async function GET(){return NextResponse.redirect(oauthUrl('https://accounts.google.com/o/oauth2/v2/auth',{client_id:env.googleClientId,redirect_uri:youtubeRedirect,response_type:'code',scope:'https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/youtube.readonly https://www.googleapis.com/auth/yt-analytics.readonly',access_type:'offline',prompt:'consent'}))}
