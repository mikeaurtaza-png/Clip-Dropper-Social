'use client';
import { Clip } from '@/lib/types';
export function ClipCard({clip,onUpdate}:{clip:Clip;onUpdate:(clip:Clip)=>void}){const color={draft:'bg-slate-500/20 text-slate-200',scheduled:'bg-blue-500/20 text-blue-200',posted:'bg-green-500/20 text-green-200',failed:'bg-red-500/20 text-red-200'}[clip.status];return <div className="card rounded-2xl p-5 space-y-4"><div className="flex items-start justify-between gap-3"><div><div className="font-bold">{clip.filename}</div><div className="text-xs text-slate-400">{clip.dropboxPath}</div></div><span className={`text-xs px-3 py-1 rounded-full ${color}`}>{clip.status}</span></div><div className="grid md:grid-cols-2 gap-3 text-sm"><div className="pill rounded-xl p-3"><div className="text-slate-400">YouTube</div><div className="font-semibold mt-1">{clip.youtubeTitle || 'No title yet'}</div><div className="text-slate-400 mt-2">Views: {clip.ytViews.toLocaleString()}</div></div><div className="pill rounded-xl p-3"><div className="text-slate-400">Facebook</div><div className="font-semibold mt-1">{clip.facebookCaption || 'No caption yet'}</div><div className="text-slate-400 mt-2">Views: {clip.fbViews.toLocaleString()}</div></div></div><div className="grid md:grid-cols-3 gap-2"><button onClick={()=>onUpdate({...clip,status:'scheduled',scheduledAt:new Date(Date.now()+3600000).toISOString()})} className="rounded-xl bg-blue-600 px-4 py-2 font-semibold">Schedule +1hr</button><button onClick={()=>onUpdate({...clip,status:'posted',ytViews:clip.ytViews||Math.floor(Math.random()*20000),fbViews:clip.fbViews||Math.floor(Math.random()*12000),youtubeUrl:'https://youtube.com/shorts/demo',facebookUrl:'https://facebook.com/reel/demo'})} className="rounded-xl bg-green-600 px-4 py-2 font-semibold">Mark Posted</button><button onClick={()=>onUpdate({...clip,status:'failed'})} className="rounded-xl bg-red-600 px-4 py-2 font-semibold">Mark Failed</button></div></div>}
